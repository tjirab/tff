gateways:
  local:
    connection:
      type: duckdb

default_gateway: local

model_defaults:
  dialect: duckdb

linter:
  enabled: true
  rules:
    - classificationmacros
    - sqlcomplexity
    - customdummyrule
